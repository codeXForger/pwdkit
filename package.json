{
  "name": "pwdkit",
  "description": "A lightweight utility to evaluate password strength and generate password suggestions.",
  "version": "1.0.4",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsup src/index.ts --dts --format esm,cjs",
    "prepare": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/codeXForger/pwdkit"
  },
  "author": "nodexforger",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.1.0",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
}
